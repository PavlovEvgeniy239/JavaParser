package expression.exceptions;

import expression.Exp;
import expression.Set;

public class CheckedSet extends Set {
    public CheckedSet(Exp expression1, Exp expression2) {
        super(expression1, expression2);
    }
}
